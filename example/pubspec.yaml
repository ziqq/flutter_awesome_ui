name: example
description: A example awesome input flutter project.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_awesome_ui:
    path:
      ../

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
